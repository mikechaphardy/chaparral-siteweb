{
  "name": "chaparral-siteweb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "css": "tailwindcss -c tailwind.config.js -i src/input.css -o public/assets/styles.css --minify",
    "dev": "npm run css && astro dev",
    "build": "npm run css && astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^5.1.0",
    "tailwindcss": "^3.4.17"
  }
}
